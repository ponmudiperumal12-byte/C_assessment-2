#include <stdio.h>
int main()
{
    int a, f, s;
    printf("enter the two digit number: ");
    scanf("%d",&a);
    f = a %10;
    s = a/ 10;
    if(f == 0 || f== 1 || s== 0 || s == 1)
        printf("0");
    else
        printf("1");
}
